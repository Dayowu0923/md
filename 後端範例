# Task 63079: 訊息發送增加使用者動作選項開發需求分析

## 📋 需求說明

### 問題描述
根據 Azure DevOps Task 63079 需求描述：公告系統在進行 CRUD 操作時，需要讓使用者選擇是否要執行特定動作（目前為郵件發送），並將這些選擇附加到事件訊息中，讓 consumer 自行決定後續行為。

### 功能需求
在以下 API 新增一個陣列欄位，讓使用者勾選要執行什麼動作（多選，但目前只有 Mail）：
1. 建立公告: `POST /announcement`, `/download`
2. 更新公告內容: `PUT /announcement/{guid}/content`, `/download/{guid}/content`
3. 更新公告檔案: `PUT /announcement/{guid}/files`, `/download/{guid}/files`
4. 刪除公告: `DELETE /announcement/{guid}`, `/download/{guid}`

### 技術要求
- 在要發送訊息時，將 API 傳送的陣列欄位附加在訊息裡面
- Consumer 接收後自行決定後續行為
- 最小變動原則：重用現有的 CQRS 架構和事件系統

## 🔍 現有架構分析

### 現有程式碼結構
系統已經有完整的 CQRS 架構：
- **Controller**: 使用 IGetService/ICommandService 進行 CQRS 分離
- **Commands**: CreateAnnouncementBoardCommand, UpdateAnnouncementBoardContentCommand, UpdateAnnouncementBoardFilesCommand, DeleteAnnouncementBoardCommand
- **Events**: AnnouncementCreatedEvent, AnnouncementContentUpdatedEvent, AnnouncementFilesUpdatedEvent, AnnouncementDeletedEvent
- **ViewModels**: CreateAnnouncementBoardViewModel, UpdateAnnouncementBoardContentViewModel, UpdateAnnouncementBoardFilesViewModel

### 現有事件系統
系統已有完整的事件發布機制：
- IEventPublisher 介面
- SlimMessageBusEventPublisher 實作
- 各種事件 record 類別已定義完成

### 可重用元件
- 現有的 CQRS 流程：Controller → Command → CommandService → Event
- 現有的事件發布機制可直接擴展
- Record 類別結構支援新增欄位

## 💡 解決方案設計

### 設計原則
1. **最小變動**: 在現有架構基礎上擴展，不改變核心流程
2. **向前相容**: 新增可選欄位，不影響現有功能
3. **擴展性**: 使用 enum 和陣列設計，支援未來新增其他動作類型

### 架構設計
```
API Request (含 PostActions[]) 
→ ViewModel (新增 PostActions 欄位)
→ Command (新增 PostActions 欄位) 
→ CommandService (傳遞到 Event)
→ Event (新增 PostActions 欄位)
→ Consumer (根據 PostActions 決定行為)
```

## 🛠️ 實作規劃

### 1. 事件後續動作類型定義

建立 `PostActionType` enum：

```csharp
namespace Announcement.Core;

/// <summary>
/// 事件後續處理動作類型
/// </summary>
public enum PostActionType
{
    /// <summary>
    /// 發送郵件通知
    /// </summary>
    Mail = 1,
    
    /// <summary>
    /// 發送 Teams 通知 (預留)
    /// </summary>
    Teams = 2,
    
    /// <summary>
    /// 發送 SMS 通知 (預留)
    /// </summary>
    Sms = 3
}
```

### 2. ViewModel 層修改

#### CreateAnnouncementBoardViewModel
```csharp
public class CreateAnnouncementBoardViewModel : IValidatableObject
{
    // ...existing properties...
    
    /// <summary>
    /// 事件後續處理動作類型清單
    /// </summary>
    public PostActionType[] PostActions { get; init; } = [];
}
```

#### UpdateAnnouncementBoardContentViewModel
```csharp
public class UpdateAnnouncementBoardContentViewModel
{
    // ...existing properties...
    
    /// <summary>
    /// 事件後續處理動作類型清單
    /// </summary>
    public PostActionType[] PostActions { get; init; } = [];
}
```

#### UpdateAnnouncementBoardFilesViewModel
```csharp
public class UpdateAnnouncementBoardFilesViewModel : IValidatableObject
{
    // ...existing properties...
    
    /// <summary>
    /// 事件後續處理動作類型清單
    /// </summary>
    public PostActionType[] PostActions { get; init; } = [];
}
```

### 3. Command 層修改

#### CreateAnnouncementBoardCommand
```csharp
public record CreateAnnouncementBoardCommand(
    string SourceSite,
    AnnouncementBoardType AnnouncementBoardType,
    string Title,
    string Content,
    DateOnly PublishDate,
    IReadOnlyList<AnnouncementBoardFileInputInfo> Files,
    string UserId,
    string UserName,
    IReadOnlyList<PostActionType> PostActions);
```

#### UpdateAnnouncementBoardContentCommand
```csharp
public record UpdateAnnouncementBoardContentCommand(
    string SourceSite,
    AnnouncementBoardType AnnouncementBoardType,
    Guid AnnouncementId,
    string Title,
    string Content,
    DateOnly PublishDate,
    string UserId,
    string UserName,
    IReadOnlyList<PostActionType> PostActions);
```

#### UpdateAnnouncementBoardFilesCommand
```csharp
public record UpdateAnnouncementBoardFilesCommand(
    string SourceSite,
    AnnouncementBoardType AnnouncementBoardType,
    Guid AnnouncementId,
    DateOnly PublishDate,
    IReadOnlyList<AnnouncementBoardFileInputInfo> Files,
    string UserId,
    string UserName,
    IReadOnlyList<PostActionType> PostActions);
```

#### DeleteAnnouncementBoardCommand
```csharp
public record DeleteAnnouncementBoardCommand(
    string SourceSite,
    AnnouncementBoardType AnnouncementBoardType,
    Guid AnnouncementId,
    string UserId,
    string UserName,
    IReadOnlyList<PostActionType> PostActions);
```

### 4. Event 層修改

#### AnnouncementCreatedEvent
```csharp
public record AnnouncementCreatedEvent(
    string EventType,
    DateTime Timestamp,
    string SourceSite,
    string BoardType,
    Guid AnnouncementId,
    string Title,
    string Content,
    DateOnly PublishDate,
    IReadOnlyList<FileData> Files,
    UserInfo CreateUser,
    DateTime CreateDatetime,
    IReadOnlyList<PostActionType> PostActions);
```

#### AnnouncementContentUpdatedEvent
```csharp
public record AnnouncementContentUpdatedEvent(
    string EventType,
    DateTime Timestamp,
    string SourceSite,
    string BoardType,
    Guid AnnouncementId,
    ContentChanges Changes,
    UserInfo UpdateUser,
    DateTime UpdateDatetime,
    IReadOnlyList<PostActionType> PostActions);
```

#### AnnouncementFilesUpdatedEvent
```csharp
public record AnnouncementFilesUpdatedEvent(
    string EventType,
    DateTime Timestamp,
    string SourceSite,
    string BoardType,
    Guid AnnouncementId,
    FileChanges FileChanges,
    UserInfo UpdateUser,
    DateTime UpdateDatetime,
    IReadOnlyList<PostActionType> PostActions);
```

#### AnnouncementDeletedEvent
```csharp
public record AnnouncementDeletedEvent(
    string EventType,
    DateTime Timestamp,
    string SourceSite,
    string BoardType,
    Guid AnnouncementId,
    string Title,
    string Content,
    DateOnly PublishDate,
    IReadOnlyList<FileData> Files,
    UserInfo DeleteUser,
    DateTime DeleteDatetime,
    IReadOnlyList<PostActionType> PostActions);
```

### 5. Controller 層修改

在 `AnnouncementBoardController.cs` 中修改各個 Action 方法，傳遞 PostActions 到對應的 Command：

#### CreateAnnouncementBoard
```csharp
var command = new CreateAnnouncementBoardCommand(
    sourceSite,
    createViewModel.AnnouncementBoardType,
    createViewModel.Title,
    createViewModel.Content,
    createViewModel.PublishDate,
    fileInputInfoItems,
    userId,
    userName,
    createViewModel.PostActions.ToList());
```

#### UpdateAnnouncementBoardContentById
```csharp
var command = new UpdateAnnouncementBoardContentCommand(
    sourceSite,
    announcementBoardType,
    announcementId,
    updateViewModel.Title,
    updateViewModel.Content,
    updateViewModel.PublishDate,
    userId,
    userName,
    updateViewModel.PostActions.ToList());
```

#### UpdateAnnouncementBoardFilesById
```csharp
var command = new UpdateAnnouncementBoardFilesCommand(
    sourceSite,
    updateViewModel.AnnouncementBoardType,
    updateViewModel.AnnouncementId,
    updateViewModel.PublishDate,
    fileInputInfoItems,
    userId,
    userName,
    updateViewModel.PostActions.ToList());
```

#### DeleteAnnouncementBoardById (Query Parameter 方式)
```csharp
[HttpDelete("{announcementBoardType:announcementBoardTypeEnum}/{announcementId:guid}")]
public async Task<IActionResult> DeleteAnnouncementBoardById(
    [FromServices] ICommandService<DeleteAnnouncementBoardCommand> commandService,
    [FromRoute] AnnouncementBoardType announcementBoardType,
    [FromRoute] Guid announcementId,
    [FromHeader(Name = "Source-Site")] string sourceSite,
    [FromQuery] PostActionType[] postActions = default) // Query Parameter 接收
{
    var userId = HttpContext.User.GetUserId()!;
    var userName = HttpContext.User.GetUserName()!;

    var command = new DeleteAnnouncementBoardCommand(
        sourceSite,
        announcementBoardType,
        announcementId,
        userId,
        userName,
        postActions?.ToList() ?? new List<PostActionType>());

    await commandService.ExecuteAsync(command, HttpContext.RequestAborted);
    return Ok();
}
```

### 6. CommandService 層修改

在各個 CommandService 的 ExecuteAsync 方法中，將 PostActions 傳遞到事件：

#### CreateAnnouncementBoardCommandService
```csharp
var eventData = new AnnouncementCreatedEvent(
    "AnnouncementCreated",
    DateTime.UtcNow,
    command.SourceSite,
    command.AnnouncementBoardType.ToString(),
    announcementId,
    command.Title,
    command.Content,
    command.PublishDate,
    fileDataList,
    new UserInfo(command.UserId, command.UserName),
    DateTime.UtcNow,
    command.PostActions);
```

其他 CommandService 也需要類似的修改。

## 📊 影響範圍

### 需要修改的檔案
1. **Announcement.Core 專案**:
   - 新增 `PostActionType.cs` enum
   - 修改 4 個 Command record 類別
   - 修改 4 個 Event record 類別
   - 修改 4 個 CommandService 類別

2. **Announcement.Web 專案**:
   - 修改 3 個 ViewModel 類別
   - 修改 `AnnouncementBoardController.cs`

### 向前相容性
- 新增欄位使用預設值 `[]`，不影響現有功能
- Consumer 可以檢查 PostActions 是否為空來判斷是否支援此功能
- 現有的事件處理邏輯可以忽略新欄位

### API 變更
所有相關 API 的 request body 都會新增可選的 `postActions` 陣列欄位：

```json
// POST /boards/announcement
{
  "title": "標題",
  "content": "內容",
  "publishDate": "2025-09-23",
  "postActions": ["Mail"]  // 新增欄位，可選
}

// PUT /boards/announcement/{id}/content
{
  "title": "新標題",
  "content": "新內容", 
  "publishDate": "2025-09-23",
  "postActions": ["Mail"]  // 新增欄位，可選
}

// PUT /boards/announcement/{id}/files (multipart/form-data)
Content-Type: multipart/form-data

publishDate: 2025-09-23
postActions: Mail
files: [file1.pdf, file2.pdf]

// DELETE /boards/announcement/{id} (Query Parameter)
DELETE /boards/announcement/12345?postActions=Mail
DELETE /boards/announcement/12345?postActions=Mail&postActions=Teams  // 多個值
```

## 🔧 實作注意事項

### 1. DELETE API 的 PostActions 傳遞
DELETE 請求通常不包含 request body，推薦使用 **Query Parameter** 方式：

**實作方式：**
```csharp
[HttpDelete("{announcementBoardType:announcementBoardTypeEnum}/{announcementId:guid}")]
public async Task<IActionResult> DeleteAnnouncementBoardById(
    [FromServices] ICommandService<DeleteAnnouncementBoardCommand> commandService,
    [FromRoute] AnnouncementBoardType announcementBoardType,
    [FromRoute] Guid announcementId,
    [FromHeader(Name = "Source-Site")] string sourceSite,
    [FromQuery] PostActionType[] postActions = default) // Query Parameter 接收
```

**API 呼叫範例：**
```bash
# 單一動作
DELETE /boards/announcement/12345?postActions=Mail

# 多個動作
DELETE /boards/announcement/12345?postActions=Mail&postActions=Teams

# 無動作（預設值）
DELETE /boards/announcement/12345
```

**優點：**
- 符合 RESTful 設計原則
- 所有 HTTP 客戶端都支援
- URL 參數清晰易讀
- ASP.NET Core 自動支援陣列參數綁定

### 2. Swagger 文件更新
新增的 PostActionType 陣列欄位會自動出現在 Swagger 文件中，需要確保：
- enum 值正確顯示
- 欄位標示為可選
- 提供適當的說明文字

### 3. 預設值處理
所有新增的 PostActions 欄位都使用 `[]` 作為預設值，確保：
- ViewModel 中設定預設值
- Command 和 Event 中使用 IReadOnlyList<PostActionType>
- CommandService 中正確傳遞空陣列到事件

### 4. 驗證邏輯
考慮是否需要新增驗證：
- PostActions 陣列不為 null
- PostActions 中的值都是有效的 enum 值
- 是否允許空陣列（建議允許）

## ✅ 驗收標準

### 功能測試
- [ ] 建立公告時可以選擇 PostActions，事件中包含正確的 PostActions 資訊
- [ ] 更新公告內容時可以選擇 PostActions，事件中包含正確的 PostActions 資訊  
- [ ] 更新公告檔案時可以選擇 PostActions，事件中包含正確的 PostActions 資訊
- [ ] 刪除公告時可以透過 Query Parameter 選擇 PostActions，事件中包含正確的 PostActions 資訊
- [ ] 不傳遞 PostActions 時使用預設值（空陣列），功能正常運作
- [ ] Consumer 可以根據 PostActions 決定是否執行郵件發送等後續動作

### API 測試
- [ ] 所有 API 的 Swagger 文件正確顯示新欄位
- [ ] JSON 請求/回應中 enum 正確序列化為字串
- [ ] DELETE API 的 Query Parameter 正確接收 PostActions 陣列
- [ ] 多個 PostActions 值可透過重複的 query parameter 正確傳遞

### 相容性測試
- [ ] 現有的 API 呼叫（不包含 PostActions）仍然正常運作
- [ ] 現有的事件處理邏輯不受影響
- [ ] 資料庫操作不受影響

### 效能測試
- [ ] 新增欄位不影響 API 回應時間
- [ ] 事件發送效能不受顯著影響

## 🚀 部署注意事項

### 1. 無資料庫變更
此變更只涉及 API 介面和事件訊息格式，不需要資料庫結構變更。

### 2. 向前相容
新版本可以與舊版本的 Consumer 共存，Consumer 可以選擇忽略新的 PostActions 欄位。

### 3. 監控重點
- 監控事件訊息大小是否有顯著增加
- 監控 Consumer 處理新格式事件的情況
- 監控 API 錯誤率是否有異常

---

**開發需求分析產生日期**: 2025-09-23  
**對應任務**: Task 63079 - [後端] 訊息發送需增加是否需要發送郵件選項  
**設計原則**: 最小變動、向前相容、可擴展性
